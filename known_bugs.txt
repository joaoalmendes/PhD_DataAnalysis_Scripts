File describing the known bugs of the scripts.

"plot_csv_data.py"

    - "no-split" is redundant since if one does not use "branches" it has the same outcome. This should be changed to an argument
    named "split" where seperate plots, e.g. one for coldown and one for warmup, are plotted and ideally analysed seperately.
    This will allow for discrete analysis and plotting of warmup and cooling down cycles which can provide very different results
    depending on the sample and therefore be interesting and usefull to analyse seperately. 











